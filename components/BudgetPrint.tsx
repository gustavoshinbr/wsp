import { brl } from "@/lib/money";
export function budgetHtml(budget: any){
  const rows = budget.items.map((i:any)=>`<tr><td>${i.imageUrl ? `<img src="${i.imageUrl}" style="width:48px;height:48px;object-fit:cover;border-radius:10px;margin-right:10px;vertical-align:middle"/>` : ""}${i.description}</td><td>${i.quantity}</td><td>${brl(i.unitValue)}</td><td>${brl(i.total)}</td></tr>`).join("");
  return `<!doctype html><html><head><meta charset="utf-8"><title>Orçamento WSP Racing</title><style>body{font-family:Arial;padding:32px;color:#111}.box{border:2px solid #111;border-radius:18px;padding:24px}h1{margin:0;color:#d90429}table{width:100%;border-collapse:collapse;margin-top:20px} img{border:1px solid #ddd}td,th{border-bottom:1px solid #ddd;padding:10px;text-align:left}.total{text-align:right;font-size:24px;font-weight:800;margin-top:20px}</style></head><body><div class="box"><h1>WSP Racing</h1><p>Orçamento #${budget.id}</p><p><b>Cliente:</b> ${budget.customer.name} - ${budget.customer.phone ?? ""}</p><table><thead><tr><th>Item</th><th>Qtd</th><th>Valor</th><th>Total</th></tr></thead><tbody>${rows}</tbody></table><p class="total">Total: ${brl(budget.total)}</p></div><script>window.print()</script></body></html>`
}
